export const coding_javascript = {
  title: "JavaScript Coding",
  subtitle: "30 Core JavaScript Interview Problems",
  isCoding: true,
  questions: [
    {
      id: "js-code-1",
      title: "Reverse a String",
      difficulty: "Easy",
      topics: ["String", "Algorithms"],
      problemStatement: "Write a function that reverses a string.",
      examples: [{ input: "'hello'", output: "'olleh'", explanation: "The reversed string is 'olleh'." }],
      constraints: ["1 <= str.length <= 10^5"],
      hints: ["Try using two pointers or the built-in reverse method."],
      approach: "Split the string into an array, reverse the array, and join it back. Alternatively, iterate backwards and concatenate.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(N)",
      solution: "function reverse(s) { return s.split('').reverse().join(''); }",
      optimizedSolution: "function reverseOpt(s) { let r = ''; for(let i = s.length-1; i>=0; i--) r += s[i]; return r; }",
      commonMistakes: ["Forgetting that strings are immutable in JavaScript."],
      followUp: ["Can you do it in-place if it was an array of characters?"]
    },
    {
      id: "js-code-2",
      title: "Check Palindrome",
      difficulty: "Easy",
      topics: ["String", "Algorithms"],
      problemStatement: "Given a string, return true if the string is a palindrome or false otherwise.",
      examples: [{ input: "'racecar'", output: "true", explanation: "Reads the same forwards and backwards." }],
      constraints: ["1 <= str.length <= 10^5"],
      hints: ["Use two pointers moving from outside in."],
      approach: "Use two pointers, one at the start and one at the end, comparing characters and moving inwards.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(1)",
      solution: "function isPalindrome(s) { return s === s.split('').reverse().join(''); }",
      optimizedSolution: "function isPalindromeOpt(s) { let l = 0, r = s.length - 1; while(l < r) { if(s[l] !== s[r]) return false; l++; r--; } return true; }",
      commonMistakes: ["Not ignoring case or spaces if required by variations."],
      followUp: ["How to handle unicode characters?"]
    },
    {
      id: "js-code-3",
      title: "Find Largest Number",
      difficulty: "Easy",
      topics: ["Array"],
      problemStatement: "Find the maximum number in an array of numbers.",
      examples: [{ input: "[1, 5, 3, 9, 2]", output: "9", explanation: "9 is the largest element." }],
      constraints: ["1 <= arr.length <= 10^5"],
      hints: ["Math.max might exceed call stack size for very large arrays. Use a loop."],
      approach: "Iterate through the array, keeping track of the maximum value seen so far.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(1)",
      solution: "function findLargest(arr) { return Math.max(...arr); }",
      optimizedSolution: "function findLargestOpt(arr) { let max = -Infinity; for (let n of arr) if (n > max) max = n; return max; }",
      commonMistakes: ["Using Math.max(...arr) on arrays with millions of elements."],
      followUp: ["What if the array is empty?"]
    },
    {
      id: "js-code-4",
      title: "Find Second Largest Number",
      difficulty: "Easy",
      topics: ["Array"],
      problemStatement: "Find the second largest unique number in an array.",
      examples: [{ input: "[10, 5, 10]", output: "5", explanation: "5 is the second largest unique number." }],
      constraints: ["2 <= arr.length <= 10^5"],
      hints: ["Keep track of the largest and second largest while iterating."],
      approach: "Initialize max and secondMax. Iterate and update both based on comparisons.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(1)",
      solution: "function secondLargest(arr) { const unique = [...new Set(arr)].sort((a,b)=>b-a); return unique[1]; }",
      optimizedSolution: "function secondLargestOpt(arr) { let max = -Infinity, second = -Infinity; for(let n of arr){ if(n > max){ second = max; max = n; } else if(n > second && n !== max) second = n; } return second; }",
      commonMistakes: ["Not handling duplicate maximum values correctly."],
      followUp: ["Find the k-th largest element."]
    },
    {
      id: "js-code-5",
      title: "Find Missing Number",
      difficulty: "Easy",
      topics: ["Array", "Math"],
      problemStatement: "Given an array containing n distinct numbers taken from 0, 1, 2, ..., n, find the one that is missing.",
      examples: [{ input: "[3,0,1]", output: "2", explanation: "2 is missing from the range [0, 3]." }],
      constraints: ["1 <= arr.length <= 10^4"],
      hints: ["Use the formula for the sum of the first n numbers."],
      approach: "Calculate expected sum using n*(n+1)/2. Subtract actual sum of array elements to find the missing one.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(1)",
      solution: "function findMissing(arr) { const n = arr.length; const expectedSum = (n * (n + 1)) / 2; const actualSum = arr.reduce((a,b)=>a+b, 0); return expectedSum - actualSum; }",
      optimizedSolution: "function findMissingOpt(arr) { let res = arr.length; for(let i=0; i<arr.length; i++) res ^= i ^ arr[i]; return res; }",
      commonMistakes: ["Integer overflow if n is extremely large (though rare in JS as numbers are double precision floats)."],
      followUp: ["Can you do it using XOR?"]
    },
    {
      id: "js-code-6",
      title: "Remove Duplicate Elements",
      difficulty: "Easy",
      topics: ["Array", "Set"],
      problemStatement: "Remove duplicate elements from an array.",
      examples: [{ input: "[1, 2, 2, 3]", output: "[1, 2, 3]", explanation: "Duplicates are removed." }],
      constraints: ["1 <= arr.length <= 10^5"],
      hints: ["Use a Set."],
      approach: "Convert the array to a Set to remove duplicates, then convert it back to an array.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(N)",
      solution: "function removeDuplicates(arr) { return arr.filter((item, index) => arr.indexOf(item) === index); }",
      optimizedSolution: "function removeDuplicatesOpt(arr) { return [...new Set(arr)]; }",
      commonMistakes: ["Using indexOf in a loop which makes it O(N^2)."],
      followUp: ["How to remove duplicates from an array of objects based on a property?"]
    },
    {
      id: "js-code-7",
      title: "Count Character Frequency",
      difficulty: "Easy",
      topics: ["String", "Hash Map"],
      problemStatement: "Count the frequency of each character in a given string.",
      examples: [{ input: "'hello'", output: "{h:1, e:1, l:2, o:1}", explanation: "Frequency map of characters." }],
      constraints: ["1 <= str.length <= 10^5"],
      hints: ["Use an object or Map to store frequencies."],
      approach: "Iterate over the string and populate an object with character counts.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(K)",
      solution: "function charFreq(str) { const freq = {}; for (let char of str) { freq[char] = (freq[char] || 0) + 1; } return freq; }",
      optimizedSolution: "function charFreqOpt(str) { return str.split('').reduce((acc, char) => { acc[char] = (acc[char] || 0) + 1; return acc; }, {}); }",
      commonMistakes: ["Not considering spaces or special characters if they need to be excluded."],
      followUp: ["Return the most frequent character."]
    },
    {
      id: "js-code-8",
      title: "Check Anagram",
      difficulty: "Easy",
      topics: ["String", "Hash Map"],
      problemStatement: "Check if two strings are anagrams of each other.",
      examples: [{ input: "s='listen', t='silent'", output: "true", explanation: "They contain the same characters in different order." }],
      constraints: ["1 <= s.length, t.length <= 10^5"],
      hints: ["Sort both strings or use a character frequency map."],
      approach: "Create a frequency map for the first string, then decrement frequencies using the second string. If all counts are zero, they are anagrams.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(1) (since alphabet is fixed)",
      solution: "function isAnagram(s, t) { return s.split('').sort().join('') === t.split('').sort().join(''); }",
      optimizedSolution: "function isAnagramOpt(s, t) { if(s.length !== t.length) return false; const map = {}; for(let char of s) map[char] = (map[char]||0) + 1; for(let char of t) { if(!map[char]) return false; map[char]--; } return true; }",
      commonMistakes: ["Assuming sorting is O(N). It's O(N log N)."],
      followUp: ["What if the inputs contain unicode characters?"]
    },
    {
      id: "js-code-9",
      title: "Fibonacci Series",
      difficulty: "Easy",
      topics: ["Math", "Recursion", "DP"],
      problemStatement: "Return the nth Fibonacci number.",
      examples: [{ input: "5", output: "5", explanation: "Sequence: 0, 1, 1, 2, 3, 5" }],
      constraints: ["0 <= n <= 50"],
      hints: ["Use an iterative approach or recursion with memoization."],
      approach: "Iteratively calculate the next Fibonacci number by adding the two previous ones.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(1)",
      solution: "function fib(n) { if(n <= 1) return n; return fib(n-1) + fib(n-2); }",
      optimizedSolution: "function fibOpt(n) { if (n <= 1) return n; let a = 0, b = 1, next; for (let i = 2; i <= n; i++) { next = a + b; a = b; b = next; } return b; }",
      commonMistakes: ["Using plain recursion which leads to O(2^N) time complexity."],
      followUp: ["Return an array of the first N Fibonacci numbers."]
    },
    {
      id: "js-code-10",
      title: "Factorial",
      difficulty: "Easy",
      topics: ["Math", "Recursion"],
      problemStatement: "Calculate the factorial of a given non-negative integer.",
      examples: [{ input: "5", output: "120", explanation: "5 * 4 * 3 * 2 * 1 = 120" }],
      constraints: ["0 <= n <= 170"],
      hints: ["Use a simple loop or recursion."],
      approach: "Multiply numbers from 1 to n.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(1)",
      solution: "function factorial(n) { if(n === 0 || n === 1) return 1; return n * factorial(n-1); }",
      optimizedSolution: "function factorialOpt(n) { let res = 1; for (let i = 2; i <= n; i++) res *= i; return res; }",
      commonMistakes: ["Not returning 1 for n=0.", "Call stack exceeded for large N in recursion."],
      followUp: ["How to handle very large numbers where JS precision is lost? (Use BigInt)"]
    },
    {
      id: "js-code-11",
      title: "Prime Number",
      difficulty: "Easy",
      topics: ["Math"],
      problemStatement: "Check if a given number is a prime number.",
      examples: [{ input: "7", output: "true", explanation: "7 has no divisors other than 1 and itself." }],
      constraints: ["1 <= n <= 10^9"],
      hints: ["Check divisibility up to the square root of n."],
      approach: "If n <= 1, return false. Loop from 2 to Math.sqrt(n). If n is divisible by any, return false.",
      timeComplexity: "O(sqrt(N))",
      spaceComplexity: "O(1)",
      solution: "function isPrime(n) { if(n <= 1) return false; for(let i=2; i<n; i++) { if(n % i === 0) return false; } return true; }",
      optimizedSolution: "function isPrimeOpt(n) { if (n <= 1) return false; if (n <= 3) return true; if (n % 2 === 0 || n % 3 === 0) return false; for (let i = 5; i * i <= n; i += 6) { if (n % i === 0 || n % (i + 2) === 0) return false; } return true; }",
      commonMistakes: ["Checking divisibility up to N instead of sqrt(N)."],
      followUp: ["Find all prime numbers up to N (Sieve of Eratosthenes)."]
    },
    {
      id: "js-code-12",
      title: "FizzBuzz",
      difficulty: "Easy",
      topics: ["Math", "String"],
      problemStatement: "Print numbers from 1 to n. For multiples of 3, print 'Fizz'. For multiples of 5, print 'Buzz'. For multiples of both, print 'FizzBuzz'.",
      examples: [{ input: "5", output: "['1','2','Fizz','4','Buzz']", explanation: "" }],
      constraints: ["1 <= n <= 10^4"],
      hints: ["Use modulo operator %."],
      approach: "Loop from 1 to n. Check modulo 15 first, then 3, then 5.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(N)",
      solution: "function fizzBuzz(n) { let result = []; for (let i = 1; i <= n; i++) { if (i % 15 === 0) result.push('FizzBuzz'); else if (i % 3 === 0) result.push('Fizz'); else if (i % 5 === 0) result.push('Buzz'); else result.push(i.toString()); } return result; }",
      optimizedSolution: "function fizzBuzzOpt(n) { let res = []; for(let i=1; i<=n; i++){ let str = ''; if(i%3===0) str += 'Fizz'; if(i%5===0) str += 'Buzz'; res.push(str || String(i)); } return res; }",
      commonMistakes: ["Checking modulo 3 and modulo 5 separately but failing to check modulo 15 (or both)."],
      followUp: ["How to structure it so adding new conditions (like 7 for 'Bazz') is easy?"]
    },
    {
      id: "js-code-13",
      title: "Reverse Words in a Sentence",
      difficulty: "Easy",
      topics: ["String"],
      problemStatement: "Reverse the order of words in a given sentence.",
      examples: [{ input: "'hello world'", output: "'world hello'", explanation: "The words are reversed." }],
      constraints: ["1 <= str.length <= 10^4"],
      hints: ["Split by space, reverse, and join."],
      approach: "Use split(' '), then reverse the array, then join(' '). Ensure multiple spaces are handled if needed.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(N)",
      solution: "function reverseWords(s) { return s.split(' ').reverse().join(' '); }",
      optimizedSolution: "function reverseWordsOpt(s) { return s.trim().split(/\\s+/).reverse().join(' '); }",
      commonMistakes: ["Not trimming leading/trailing spaces or handling multiple spaces between words."],
      followUp: ["Reverse the characters in each word but keep word order?"]
    },
    {
      id: "js-code-14",
      title: "Flatten Nested Array",
      difficulty: "Medium",
      topics: ["Array", "Recursion"],
      problemStatement: "Flatten an array of arbitrarily nested arrays of integers into a flat array.",
      examples: [{ input: "[1, [2, [3, 4], 5]]", output: "[1, 2, 3, 4, 5]", explanation: "The array is completely flattened." }],
      constraints: ["0 <= depth <= 100"],
      hints: ["Use recursion or Array.prototype.flat() if allowed."],
      approach: "Iterate over array elements. If an element is an array, recursively flatten it. Otherwise, add it to result.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(N)",
      solution: "function flatten(arr) { return arr.flat(Infinity); }",
      optimizedSolution: "function flattenOpt(arr) { const res = []; for(let el of arr){ if(Array.isArray(el)) res.push(...flattenOpt(el)); else res.push(el); } return res; }",
      commonMistakes: ["Not flattening deeply enough (only 1 level)."],
      followUp: ["Implement it iteratively using a stack."]
    },
    {
      id: "js-code-15",
      title: "Deep Copy Object",
      difficulty: "Medium",
      topics: ["Object", "Recursion"],
      problemStatement: "Create a deep clone of a nested object or array.",
      examples: [{ input: "{a: 1, b: {c: 2}}", output: "{a: 1, b: {c: 2}}", explanation: "Returns a new identical object." }],
      constraints: ["No circular references in standard cases."],
      hints: ["JSON.parse(JSON.stringify(obj)) works for basic types.", "For complex objects, use recursion."],
      approach: "Recursively iterate over object keys and copy them. Handle Date, Map, Set, and circular references for a robust solution.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(N)",
      solution: "function deepCopy(obj) { return JSON.parse(JSON.stringify(obj)); }",
      optimizedSolution: "function deepCopyOpt(obj, map = new WeakMap()) { if (obj === null || typeof obj !== 'object') return obj; if (map.has(obj)) return map.get(obj); const clone = Array.isArray(obj) ? [] : {}; map.set(obj, clone); for (let key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) clone[key] = deepCopyOpt(obj[key], map); } return clone; }",
      commonMistakes: ["JSON.parse approach fails with functions, undefined, Symbols, and circular references."],
      followUp: ["How to handle circular references?"]
    },
    {
      id: "js-code-16",
      title: "Debounce Function",
      difficulty: "Medium",
      topics: ["Functions", "Closures"],
      problemStatement: "Implement a debounce function that limits the rate at which a function gets called.",
      examples: [{ input: "debounce(fn, 300)", output: "Function", explanation: "Delays invoking fn until after 300ms have elapsed since the last time it was invoked." }],
      constraints: ["0 <= wait <= 10000"],
      hints: ["Use setTimeout and clearTimeout.", "Return a closure that maintains the timer state."],
      approach: "Clear the previous timeout on every call and set a new one.",
      timeComplexity: "O(1)",
      spaceComplexity: "O(1) for closure",
      solution: "function debounce(fn, wait) { let timeoutId; return function(...args) { clearTimeout(timeoutId); timeoutId = setTimeout(() => fn.apply(this, args), wait); }; }",
      optimizedSolution: "function debounceOpt(fn, wait) { let timeoutId; return function(...args) { clearTimeout(timeoutId); timeoutId = setTimeout(() => fn.apply(this, args), wait); }; }",
      commonMistakes: ["Losing the correct 'this' context by not using apply/call.", "Not passing arguments correctly."],
      followUp: ["Add an 'immediate' flag to call it on the leading edge."]
    },
    {
      id: "js-code-17",
      title: "Throttle Function",
      difficulty: "Medium",
      topics: ["Functions", "Closures"],
      problemStatement: "Implement a throttle function that ensures a function is called at most once in a specified time period.",
      examples: [{ input: "throttle(fn, 300)", output: "Function", explanation: "Invokes fn at most once per 300ms." }],
      constraints: ["0 <= wait <= 10000"],
      hints: ["Keep track of the last time the function was executed.", "Use setTimeout for trailing edge."],
      approach: "Check if enough time has passed since the last execution. If so, call it. Otherwise, ignore.",
      timeComplexity: "O(1)",
      spaceComplexity: "O(1) for closure",
      solution: "function throttle(fn, wait) { let lastCall = 0; return function(...args) { const now = new Date().getTime(); if (now - lastCall < wait) return; lastCall = now; return fn.apply(this, args); }; }",
      optimizedSolution: "function throttleOpt(fn, wait) { let inThrottle; return function(...args) { if (!inThrottle) { fn.apply(this, args); inThrottle = true; setTimeout(() => (inThrottle = false), wait); } }; }",
      commonMistakes: ["Failing to execute the last call if it happens during the wait period (trailing edge missing)."],
      followUp: ["Implement leading and trailing options."]
    },
    {
      id: "js-code-18",
      title: "Polyfill for map()",
      difficulty: "Medium",
      topics: ["Array", "Polyfill"],
      problemStatement: "Implement your own version of Array.prototype.map.",
      examples: [{ input: "[1,2,3].myMap(x => x * 2)", output: "[2,4,6]", explanation: "Transforms each element." }],
      constraints: ["No built-in map() allowed"],
      hints: ["Add the function to Array.prototype.", "Loop through the array and apply the callback."],
      approach: "Iterate over 'this'. Apply callback with (element, index, array). Push result to new array.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(N)",
      solution: "Array.prototype.myMap = function(cb) { const result = []; for(let i=0; i<this.length; i++) { result.push(cb(this[i], i, this)); } return result; };",
      optimizedSolution: "Array.prototype.myMap = function(cb, thisArg) { const result = new Array(this.length); for(let i=0; i<this.length; i++) { if (i in this) result[i] = cb.call(thisArg, this[i], i, this); } return result; };",
      commonMistakes: ["Not handling sparse arrays.", "Not supporting the optional thisArg."],
      followUp: ["How does your implementation handle holes in the array?"]
    },
    {
      id: "js-code-19",
      title: "Polyfill for filter()",
      difficulty: "Medium",
      topics: ["Array", "Polyfill"],
      problemStatement: "Implement your own version of Array.prototype.filter.",
      examples: [{ input: "[1,2,3].myFilter(x => x > 1)", output: "[2,3]", explanation: "Filters elements based on predicate." }],
      constraints: ["No built-in filter() allowed"],
      hints: ["Loop and push to new array if callback returns truthy."],
      approach: "Iterate over 'this'. If callback returns truthy, push element to result array.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(N) (worst case)",
      solution: "Array.prototype.myFilter = function(cb) { const result = []; for(let i=0; i<this.length; i++) { if(cb(this[i], i, this)) result.push(this[i]); } return result; };",
      optimizedSolution: "Array.prototype.myFilter = function(cb, thisArg) { const result = []; for(let i=0; i<this.length; i++) { if (i in this && cb.call(thisArg, this[i], i, this)) result.push(this[i]); } return result; };",
      commonMistakes: ["Not handling sparse arrays.", "Not supporting thisArg."],
      followUp: ["Can you do it using reduce?"]
    },
    {
      id: "js-code-20",
      title: "Polyfill for reduce()",
      difficulty: "Medium",
      topics: ["Array", "Polyfill"],
      problemStatement: "Implement your own version of Array.prototype.reduce.",
      examples: [{ input: "[1,2,3].myReduce((acc, x) => acc + x, 0)", output: "6", explanation: "Reduces array to a single value." }],
      constraints: ["No built-in reduce() allowed"],
      hints: ["Handle the case where initialValue is not provided.", "Start index depends on initialValue."],
      approach: "Determine initial value and start index. Loop through array, updating accumulator with callback result.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(1)",
      solution: "Array.prototype.myReduce = function(cb, init) { let acc = init; let i = 0; if(init === undefined) { acc = this[0]; i = 1; } for(; i<this.length; i++) { acc = cb(acc, this[i], i, this); } return acc; };",
      optimizedSolution: "Array.prototype.myReduce = function(cb, init) { let i = 0, acc = init; if (init === undefined) { if (this.length === 0) throw new TypeError('Reduce of empty array with no initial value'); while(i < this.length && !(i in this)) i++; acc = this[i++]; } for(; i < this.length; i++) { if (i in this) acc = cb(acc, this[i], i, this); } return acc; };",
      commonMistakes: ["Failing when no initialValue is provided and array has 1 element.", "Not skipping empty slots in sparse arrays."],
      followUp: ["What happens when reduce is called on an empty array without initialValue?"]
    },
    {
      id: "js-code-21",
      title: "Promise Implementation",
      difficulty: "Hard",
      topics: ["Async", "Promises"],
      problemStatement: "Create a basic polyfill for the Promise class (MyPromise).",
      examples: [{ input: "new MyPromise((res, rej) => res(1))", output: "MyPromise { state: 'fulfilled', value: 1 }", explanation: "Basic promise structure." }],
      constraints: ["Implement constructor, then, catch"],
      hints: ["Manage state: pending, fulfilled, rejected.", "Store callbacks for asynchronous execution."],
      approach: "Class with state, value, and arrays for onResolve and onReject callbacks. The executor function is called immediately.",
      timeComplexity: "O(1)",
      spaceComplexity: "O(N) for storing callbacks",
      solution: "class MyPromise { constructor(exec) { this.state = 'pending'; this.value = null; const res = (val) => { if(this.state === 'pending') { this.state = 'fulfilled'; this.value = val; } }; const rej = (err) => { if(this.state === 'pending') { this.state = 'rejected'; this.value = err; } }; exec(res, rej); } }",
      optimizedSolution: "class MyPromise { constructor(exec) { this.state = 'pending'; this.value = null; this.callbacks = []; const resolve = (val) => { if(this.state !== 'pending') return; this.state = 'fulfilled'; this.value = val; this.callbacks.forEach(cb => cb.onFulfilled(val)); }; const reject = (err) => { if(this.state !== 'pending') return; this.state = 'rejected'; this.value = err; this.callbacks.forEach(cb => cb.onRejected(err)); }; try { exec(resolve, reject); } catch(err) { reject(err); } } then(onFulfilled, onRejected) { return new MyPromise((res, rej) => { /* queue microtask to handle resolution */ }); } }",
      commonMistakes: ["Not making `then` callbacks execute asynchronously (using queueMicrotask or setTimeout).", "Not chaining promises correctly in `then`."],
      followUp: ["Implement Promise.all()", "How to make callbacks execute asynchronously?"]
    },
    {
      id: "js-code-22",
      title: "Async/Await API Fetch",
      difficulty: "Easy",
      topics: ["Async", "Network"],
      problemStatement: "Write an async function that fetches data from an API and handles errors.",
      examples: [{ input: "fetchData('https://api.example.com/data')", output: "Promise<Object>", explanation: "Returns parsed JSON." }],
      constraints: ["Use fetch API"],
      hints: ["Use try/catch blocks.", "Check response.ok."],
      approach: "Await fetch call. If response is not ok, throw error. Await response.json(). Catch and handle errors.",
      timeComplexity: "O(1)",
      spaceComplexity: "O(1)",
      solution: "async function fetchData(url) { try { const res = await fetch(url); const data = await res.json(); return data; } catch(e) { console.error(e); } }",
      optimizedSolution: "async function fetchDataOpt(url) { try { const res = await fetch(url); if (!res.ok) throw new Error(`HTTP error! status: ${res.status}`); return await res.json(); } catch (error) { console.error('Fetch failed:', error.message); throw error; } }",
      commonMistakes: ["Forgetting to check response.ok. fetch doesn't throw on 404/500."],
      followUp: ["How to add a timeout to the fetch request? (Use AbortController)"]
    },
    {
      id: "js-code-23",
      title: "Implement setTimeout",
      difficulty: "Hard",
      topics: ["System Design", "Async"],
      problemStatement: "How would you implement your own setTimeout if you only had access to requestAnimationFrame or a busy loop? (Conceptual)",
      examples: [{ input: "mySetTimeout(fn, 1000)", output: "id", explanation: "Executes fn after 1s." }],
      constraints: ["No using actual setTimeout"],
      hints: ["Use Date.now() to check elapsed time in a loop (rAF or while)."],
      approach: "Create a loop using requestAnimationFrame that continually checks if the current time minus the start time is greater than the delay.",
      timeComplexity: "O(1) per frame",
      spaceComplexity: "O(1)",
      solution: "function mySetTimeout(cb, delay) { const start = Date.now(); function loop() { if (Date.now() - start >= delay) { cb(); } else { requestAnimationFrame(loop); } } requestAnimationFrame(loop); }",
      optimizedSolution: "function mySetTimeoutOpt(cb, delay) { let start; let id; function loop(time) { if (!start) start = time; if (time - start >= delay) { cb(); } else { id = requestAnimationFrame(loop); } } id = requestAnimationFrame(loop); return () => cancelAnimationFrame(id); }",
      commonMistakes: ["Using a while loop which blocks the main thread completely."],
      followUp: ["How to implement clearMyTimeout?"]
    },
    {
      id: "js-code-24",
      title: "LRU Cache (Medium)",
      difficulty: "Medium",
      topics: ["Class", "Data Structures"],
      problemStatement: "Design a data structure that follows the constraints of a Least Recently Used (LRU) cache.",
      examples: [{ input: "LRUCache(2)", output: "Instance", explanation: "Capacity of 2." }],
      constraints: ["get and put must be O(1) time."],
      hints: ["Use a Map because JS Maps preserve insertion order."],
      approach: "When getting, if exists, delete and re-insert to mark as recently used. When putting, if exists, delete first. Insert new value. If size exceeds capacity, delete the first item in the Map (using map.keys().next().value).",
      timeComplexity: "O(1)",
      spaceComplexity: "O(capacity)",
      solution: "class LRUCache { constructor(cap) { this.cache = new Map(); this.cap = cap; } get(key) { if(!this.cache.has(key)) return -1; const val = this.cache.get(key); this.cache.delete(key); this.cache.set(key, val); return val; } put(key, value) { if(this.cache.has(key)) this.cache.delete(key); this.cache.set(key, value); if(this.cache.size > this.cap) this.cache.delete(this.cache.keys().next().value); } }",
      optimizedSolution: "class LRUCacheOpt { constructor(capacity) { this.cache = new Map(); this.capacity = capacity; } get(key) { if (!this.cache.has(key)) return -1; const v = this.cache.get(key); this.cache.delete(key); this.cache.set(key, v); return v; } put(key, value) { if (this.cache.has(key)) this.cache.delete(key); this.cache.set(key, value); if (this.cache.size > this.capacity) { this.cache.delete(this.cache.keys().next().value); } } }",
      commonMistakes: ["Implementing it with Object and Array, making operations O(N)."],
      followUp: ["Implement it using a Doubly Linked List and a Hash Map."]
    },
    {
      id: "js-code-25",
      title: "Group Array by Property",
      difficulty: "Easy",
      topics: ["Array", "Object"],
      problemStatement: "Group an array of objects by a specific property.",
      examples: [{ input: "[{id: 1, cat: 'A'}, {id: 2, cat: 'B'}, {id: 3, cat: 'A'}]", output: "{ 'A': [{id: 1...}, {id:3...}], 'B': [{id: 2...}] }", explanation: "Grouped by 'cat'." }],
      constraints: ["1 <= arr.length <= 10^4"],
      hints: ["Use Array.prototype.reduce."],
      approach: "Reduce the array into an object. If the group key doesn't exist on the object, initialize it with an array. Push the item to the array.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(N)",
      solution: "function groupBy(arr, prop) { const res = {}; for(let item of arr) { const key = item[prop]; if(!res[key]) res[key] = []; res[key].push(item); } return res; }",
      optimizedSolution: "function groupByOpt(arr, prop) { return arr.reduce((acc, obj) => { const key = obj[prop]; acc[key] = acc[key] || []; acc[key].push(obj); return acc; }, {}); }",
      commonMistakes: ["Mutating the original objects unnecessarily."],
      followUp: ["Group by a function instead of a property name."]
    },
    {
      id: "js-code-26",
      title: "Merge Two Objects",
      difficulty: "Easy",
      topics: ["Object"],
      problemStatement: "Merge two objects. If properties conflict, the second object's properties should overwrite the first's.",
      examples: [{ input: "a={x:1, y:2}, b={y:3, z:4}", output: "{x:1, y:3, z:4}", explanation: "Property y is overwritten." }],
      constraints: ["Shallow merge"],
      hints: ["Use the spread operator or Object.assign."],
      approach: "Spread syntax `{...a, ...b}` or `Object.assign({}, a, b)` achieves this simply.",
      timeComplexity: "O(K)",
      spaceComplexity: "O(K)",
      solution: "function mergeObjects(a, b) { return Object.assign({}, a, b); }",
      optimizedSolution: "function mergeObjectsOpt(a, b) { return { ...a, ...b }; }",
      commonMistakes: ["Mutating object a instead of returning a new merged object."],
      followUp: ["How to perform a deep merge?"]
    },
    {
      id: "js-code-27",
      title: "Chunk Array",
      difficulty: "Medium",
      topics: ["Array"],
      problemStatement: "Split an array into smaller arrays of a specified size.",
      examples: [{ input: "arr=[1,2,3,4,5], size=2", output: "[[1,2], [3,4], [5]]", explanation: "Chunked into sizes of 2." }],
      constraints: ["1 <= size <= arr.length"],
      hints: ["Use a loop and Array.prototype.slice."],
      approach: "Loop through the array, incrementing by `size`. Push `arr.slice(i, i + size)` to the result array.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(N)",
      solution: "function chunk(arr, size) { const res = []; for(let i=0; i<arr.length; i+=size) { res.push(arr.slice(i, i+size)); } return res; }",
      optimizedSolution: "function chunkOpt(arr, size) { const chunked = []; let i = 0; while (i < arr.length) { chunked.push(arr.slice(i, i + size)); i += size; } return chunked; }",
      commonMistakes: ["Off-by-one errors in loop conditions."],
      followUp: ["Implement it without using slice."]
    },
    {
      id: "js-code-28",
      title: "Find Duplicate Elements",
      difficulty: "Easy",
      topics: ["Array", "Set"],
      problemStatement: "Find all duplicate elements in an array.",
      examples: [{ input: "[1, 2, 3, 2, 4, 3]", output: "[2, 3]", explanation: "2 and 3 appear more than once." }],
      constraints: ["1 <= arr.length <= 10^5"],
      hints: ["Use a Set or Map to keep track of seen elements."],
      approach: "Iterate over the array. If an element is already in a 'seen' Set, add it to a 'duplicates' Set to ensure duplicates aren't listed multiple times.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(N)",
      solution: "function findDuplicates(arr) { return arr.filter((item, index) => arr.indexOf(item) !== index); }",
      optimizedSolution: "function findDuplicatesOpt(arr) { const seen = new Set(); const dupes = new Set(); for(let val of arr){ if(seen.has(val)) dupes.add(val); else seen.add(val); } return [...dupes]; }",
      commonMistakes: ["Using indexOf inside filter making it O(N^2).", "Returning duplicates multiple times if they appear more than twice."],
      followUp: ["Find duplicates in O(N) time and O(1) extra space if array contains numbers from 1 to N."]
    },
    {
      id: "js-code-29",
      title: "Two Sum",
      difficulty: "Easy",
      topics: ["Array", "Hash Map"],
      problemStatement: "Given an array of integers and a target sum, return indices of the two numbers such that they add up to the target.",
      examples: [{ input: "nums=[2,7,11,15], target=9", output: "[0, 1]", explanation: "nums[0] + nums[1] == 9" }],
      constraints: ["2 <= nums.length <= 10^4", "Exactly one valid answer exists."],
      hints: ["Use a Hash Map to store the difference between target and current number."],
      approach: "Iterate over nums. Calculate complement (target - num). If complement is in map, return map[complement] and current index. Else store map[num] = index.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(N)",
      solution: "function twoSum(nums, target) { for(let i=0; i<nums.length; i++) { for(let j=i+1; j<nums.length; j++) { if(nums[i]+nums[j] === target) return [i, j]; } } }",
      optimizedSolution: "function twoSumOpt(nums, target) { const map = new Map(); for (let i = 0; i < nums.length; i++) { const complement = target - nums[i]; if (map.has(complement)) return [map.get(complement), i]; map.set(nums[i], i); } return []; }",
      commonMistakes: ["Using nested loops yielding O(N^2) time complexity."],
      followUp: ["What if the array is sorted? (Use Two Pointers)"]
    },
    {
      id: "js-code-30",
      title: "Longest Substring Without Repeating Characters",
      difficulty: "Medium",
      topics: ["String", "Sliding Window"],
      problemStatement: "Given a string, find the length of the longest substring without repeating characters.",
      examples: [{ input: "'abcabcbb'", output: "3", explanation: "The answer is 'abc', with the length of 3." }],
      constraints: ["0 <= s.length <= 5 * 10^4"],
      hints: ["Use a sliding window with two pointers."],
      approach: "Use a Set and two pointers (left, right). If s[right] is not in Set, add it, increment right, update max length. If it is in Set, remove s[left], increment left.",
      timeComplexity: "O(N)",
      spaceComplexity: "O(min(N, M)) where M is character set size",
      solution: "function lengthOfLongestSubstring(s) { let max = 0; for(let i=0; i<s.length; i++){ let set = new Set(); for(let j=i; j<s.length; j++){ if(set.has(s[j])) break; set.add(s[j]); max = Math.max(max, set.size); } } return max; }",
      optimizedSolution: "function lengthOfLongestSubstringOpt(s) { let set = new Set(), max = 0, left = 0; for(let right=0; right<s.length; right++){ while(set.has(s[right])){ set.delete(s[left]); left++; } set.add(s[right]); max = Math.max(max, right - left + 1); } return max; }",
      commonMistakes: ["Using O(N^2) or O(N^3) brute force solutions."],
      followUp: ["Optimize the sliding window by storing character indices in a Map to jump the left pointer directly."]
    }
  ]
};
